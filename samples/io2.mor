
trait Read 
    fn read = (self, buf: [u8]) -> Result<usize>
end

struct File = {
    handle: usize,
} + Read

impl File
    fn open = (String) -> Result<Self>
        let handle = windows_open_file @
        return File (handle: 1234)
    end
end

impl Read for File
    fn read = (self, (buf: [u8])) -> Result<usize>
        // do the read
    end
end

//////////

impl String
    fn split = (self, separator: String) -> Iterator<String>
        // blah
    end
end
