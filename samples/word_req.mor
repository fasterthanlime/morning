import system.io.(File, stdout)

fn main ()
    let file = File.open "book.txt"
    let contents = file.read_into_string()?

    let words = Set<String>.new



    contents.split " ".each words.add

    (contents.split " ").each words.add
    contents.split (" ".each words.add)
end
